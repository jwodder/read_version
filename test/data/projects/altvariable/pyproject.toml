[build-system]
requires = [
    "read_version ~= 0.2.0",
    "setuptools >= 42.0.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.read_version.version]
path = "foobar.py"
variable = "__no_use_this_one__"
